# MZTEK Templates

This folder stores reusable, non-sensitive templates that can be committed.

Runtime outputs belong in `.mztek/runtime/`.
Logs belong in `.mztek/logs/`.
Secrets belong in `.mztek/secure/` and must never be committed.
